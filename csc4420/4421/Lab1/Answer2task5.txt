Task 5

1) Default sort order for ls is alphabetical

2) ls -a is the capacity to list all the entries starting with . 

3) q is a legal option for ls representing hiding control characters printing ? instead of nongrapic characters

4) R is a legal option for ls meaning recursive listing all subdirectories recursively 

5) ls is inside of the /bin/ls directory 

